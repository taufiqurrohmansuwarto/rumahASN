/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function (knex) {
  return knex.raw(
    `alter table tickets_comments_customers alter column comment type text using comment::text;`
  );
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function (knex) {
  return knex.raw(
    `alter table tickets_comments_customers alter column comment type varchar(255) using comment::varchar(255);`
  );
};
