const WebinarSeriesParticipantsAbsence = require("@/models/webinar-series-participants-absence.model");

// admin
const findWebinarSeriesParticipantsAbsence = async (req, res) => {
  try {
  } catch (error) {}
};

const userAbsences = async (req, res) => {};

// user
const registerAbsence = async (req, res) => {
  try {
  } catch (error) {}
};

const unregisterAbsence = async (req, res) => {
  try {
  } catch (error) {}
};

module.exports = {
  // admin
  findWebinarSeriesParticipantsAbsence,

  // user
  registerAbsence,
  unregisterAbsence,
  userAbsences,
};
