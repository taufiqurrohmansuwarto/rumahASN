import { handleError } from "@/utils/helper/controller-helper";

const SycnPegawai = require("@/models/sync-pegawai.model");

export const dashbaord = async (req, res) => {
  try {
  } catch (error) {
    handleError(res, error);
  }
};

export const tmtCpnsNipPNS = async (req, res) => {
  try {
  } catch (error) {
    handleError(res, error);
  }
};

export const tglLahirNipASN = async (req, res) => {
  try {
  } catch (error) {
    handleError(res, error);
  }
};

export const tahunPengangkatanPPPK = async (req, res) => {
  try {
  } catch (error) {
    handleError(res, error);
  }
};
